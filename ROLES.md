# Ролевая система Car Rental Manager

## 🎯 Обзор системы ролей

Система использует **глобальные роли** для определения прав доступа пользователей в B2B-агрегаторе проката автомобилей. Каждый пользователь имеет одну глобальную роль, которая определяет его полномочия и доступ к функциям системы.

## 👥 Глобальные роли (GlobalRole)

### 1. DIRECTOR - Управляющий Автопарком  
- **Описание**: Управляет автопарком и операциями компании
- **Права**: 
  - Полный доступ к корпоративному автопарку
  - Управление менеджерами и их правами
  - Выдача прав доступа к автомобилям
  - Подтверждение и управление бронированиями
  - Просмотр всех отчетов компании
  - Может иметь личный гараж (PersonalGarage)
- **Иерархия**: Высший операционный уровень (index: 0)

### 2. MANAGER - Операционный Менеджер
- **Описание**: Сотрудник для работы с клиентами и бронированиями
- **Права**: 
  - Доступ только к авто с выданными правами (CarPermissions)
  - Создание и управление бронированиями (при наличии прав)
  - Работа с клиентами
  - Просмотр своих комиссий
  - Может иметь личный гараж (PersonalGarage)
- **Иерархия**: Операционный уровень (index: 1)

### 3. INVESTOR - Владелец-Инвестор
- **Описание**: Собственник автомобилей, инвестор в автопарк
- **Права**: 
  - Полный доступ к своим автомобилям
  - Добавление/удаление собственных авто
  - Выбор управляющей компании
  - Просмотр доходов и финансовых отчетов
  - Настройка ставки комиссии
  - Владение личным гаражом (PersonalGarage)
- **Иерархия**: Уровень владельца активов (index: 2)

### 4. GUEST - Гость
- **Описание**: Пользователь с ограниченными правами просмотра
- **Права**: 
  - Просмотр календаря аренды
  - Просмотр информации об автомобилях
  - Доступ только для чтения
  - Может владеть личным гаражом (PersonalGarage) с управлением через компанию
- **Иерархия**: Базовый уровень доступа (index: 3)

## 🏢 Принадлежность к организации

Пользователи могут быть привязаны к одной или нескольким организациям через поле `organizations`. Принадлежность определяет область видимости данных, но права доступа определяются исключительно глобальной ролью.

## 🔐 Матрица прав доступа

| Функция | DIRECTOR | MANAGER | INVESTOR | GUEST |
|---------|----------|---------|----------|-------|
| Вход в систему | ✅ | ✅ | ✅ | ✅ |
| Управление компанией | ✅ | ❌ | ❌ | ❌ |
| Приглашать MANAGER | ✅ | ❌ | ❌ | ❌ |
| Управление авто компании | ✅ | По разрешению | ❌ | ❌ |
| Управление своими авто | ✅ | ✅ | ✅ | ❌ |
| Удаление авто | ✅ (компании) | ❌ | ✅ (свои) | ❌ |
| Календарь авто | ✅ | По разрешению | ✅ (свои) | ✅ |
| Бронирование | ✅ | По разрешению | ❌ | ❌ |
| Подтверждение заявок | ✅ | ❌ | ❌ | ❌ |
| Финансовые отчеты | ✅ (компания) | ✅ (свои) | ✅ (свои) | ❌ |
| PersonalGarage | ✅ | ✅ | ✅ | ✅ (только просмотр) |

## 🛠 Использование в коде

### Проверка прав доступа

```dart
import 'package:car_rental_manager/models/models.dart';
import 'package:car_rental_manager/utils/access_control.dart';

// Проверка прав на просмотр автомобиля
final canView = await AccessControl.canViewCar(
  user: currentUser,
  car: selectedCar,
  permissions: carPermissions,
);

// Проверка прав на редактирование
final canEdit = await AccessControl.canEditCar(
  user: currentUser,
  car: selectedCar,
  permissions: carPermissions,
);

// Проверка роли
if (currentUser.globalRole == GlobalRole.director) {
  // Действия для DIRECTOR
}
```

### Создание пользователя с ролью

```dart
final newUser = AppUser(
  uid: firebaseUser.uid,
  email: 'user@example.com',
  displayName: 'John Doe',
  globalRole: GlobalRole.manager,  // DIRECTOR, MANAGER, INVESTOR, GUEST
  organizations: ['org_123'],
  createdAt: DateTime.now(),
  updatedAt: DateTime.now(),
);
```

### Добавление пользователя в организацию

```dart
// Добавить пользователя к организации
await FirebaseFirestore.instance
    .collection('users')
    .doc(userId)
    .update({
  'organizations': FieldValue.arrayUnion([organizationId])
});
```

## 🔄 Гибкая система прав через CarPermissions

Помимо глобальных ролей, система использует **гранулярные права доступа** к конкретным автомобилям через модель `CarPermissions`.

### Правила делегирования прав:

1. **canView (просмотр календаря загрузки)** - может выдать **владелец гаража** любому пользователю
2. **Операционные права** (canEdit, canBook, canConfirmBooking, canViewFinancials) - может выдать только **DIRECTOR** компании
3. Для выдачи операционных прав автомобиль должен быть передан в управление компании (companyId != null)

### Типы прав:

- `canView` - просмотр календаря загрузки автомобиля (выдает владелец гаража)
- `canEdit` - редактирование данных авто (выдает DIRECTOR)
- `canBook` - создание бронирований (выдает DIRECTOR)
- `canConfirmBooking` - подтверждение заявок на бронирование (выдает DIRECTOR)
- `canViewFinancials` - просмотр финансовых данных по авто (выдает DIRECTOR)

### Примеры использования:

```dart
// Владелец гаража выдает право просмотра календаря
await AccessControl.grantViewPermission(
  granterId: garageOwnerId,
  userId: anyUserId,
  car: car,
);

// DIRECTOR выдает операционные права менеджеру
final permissions = CarPermissions(
  id: 'perm_123',
  userId: managerId,
  carId: carId,
  canView: true,
  canEdit: false,
  canBook: true,
  canConfirmBooking: false,
  canViewFinancials: false,
  grantedBy: directorId,
  grantedAt: DateTime.now(),
);

await AccessControl.grantCarPermissions(
  granterId: directorId,
  granterRole: GlobalRole.director,
  managerId: managerId,
  car: car,
  permissions: permissions,
);
```

## 📱 Применение в UI

### Условное отображение элементов:
```dart
if (AccessControl.canEditCar(user, car, permissions)) {
  return EditCarButton();
}

// Для GUEST - только просмотр
if (user.globalRole == GlobalRole.guest) {
  return ReadOnlyCarView();
}
```

### Роутинг на основе ролей:
```dart
Widget getHomeScreen(GlobalRole role) {
  switch (role) {
    case GlobalRole.director:
      return DirectorDashboard();
    case GlobalRole.manager:
      return ManagerDashboard();
    case GlobalRole.investor:
      return InvestorDashboard();
    case GlobalRole.guest:
      return GuestCalendarView();
  }
}
```

## 🎯 Особенности ролей

### DIRECTOR
- Управляет корпоративным автопарком (CompanyGarage)
- Может иметь личный гараж (PersonalGarage) для своих авто
- Выдает права менеджерам через CarPermissions
- Полный контроль над операциями компании

### MANAGER
- Работает только с авто, на которые выданы права
- Может владеть личными авто через PersonalGarage
- Получает комиссию за бронирования

### INVESTOR
- Фокус на инвестициях и доходах
- Владеет авто через PersonalGarage
- Передает управление компании (managingCompanyId)
- Получает доход за вычетом комиссии

### GUEST
- Только просмотр данных
- Не может создавать бронирования
- Может владеть авто, но все операции через компанию
- Подходит для партнеров, аудиторов, инвесторов на раннем этапе