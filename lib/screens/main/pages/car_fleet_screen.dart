import 'package:flutter/material.dart';

class CarFleetScreen extends StatelessWidget {
  const CarFleetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Автопарк'),
      ),
    );
  }
}
